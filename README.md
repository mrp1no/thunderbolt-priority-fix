# thunderbolt-priority-fix
A macOS double-click utility for two (or more) Macs joined by a Thunderbolt cable. It blocks a peer Mac on Ethernet/Wi-Fi, bypassing macOS's automatic address selection for Bonjour file sharing and forcing Finder (which normally ignores the service order in System Settings) to use Thunderbolt Bridge instead.
